html {
  font-size: 16px;
}

@media only screen and (max-width: 992px) {
  html {
    font-size: 12px;
  }
}

@font-face {
  font-family: 'Poppins';
  src: url("{{ 'Poppins-Regular.ttf' | asset_url }}");
}

body {
  background-color: #000;
  font-family: 'Poppins';
  margin: 0;
}

textarea:focus, input:focus{
  outline: none;
}

.form-search {
  position: relative;
  margin: 0;
}

.input-search {
  background: transparent;
  color: rgba(255, 255, 255, .6);
  border: none;
  border-bottom: 1px solid rgba(255, 255, 255, .6);
  padding-right: 40px;
  width: 100%;
}

.input-search::placeholder {
  font-style: italic;
}

.input-search:hover, .input-search:focus {
  outline: none;
}

.icon-btn {
  background: transparent;
  position: absolute;
  right: 0;
  bottom: 5px;
  border: none;
}

.icon-btn:focus, .icon-btn:hover {
  outline: none;
}

.theme-btn {
  font-family: 'Poppins';
  font-style: normal;
  font-weight: 400;
  font-size: .8rem;
  line-height: 120%;
  text-transform: capitalize;
  background-color: transparent;
  color: #FFFFFF;
  border: 1px solid rgba(255, 255, 255, .5);
  border-radius: .4rem;
  padding: 0.625rem .75rem;
}

.theme-btn:hover {
  background-color: #fff;
  color: #000;
}

.theme-btn:hover, .theme-btn:focus {
  outline: none;
}

.theme-btn:hover path {
  fill: #000;
}

.cart__link {
  color: #fff;
  font-size: .875rem;
  line-height: 120%;
}

.cart__link:hover {
  color: #fff;
}

.navbar__row {
  display: flex;
  color: white;
}

.navbar__item {
  padding-right: .5rem;
}

.navbar__items {
  display: flex;
}

.navbar__item-brand {
  display: flex;
  align-items: center;
}

.navbar__items--right {
  display: flex;
  align-items: center;
  margin-left: auto;
}

.navbar__item-search {
  margin-right: 1rem;
}

.navbar__item-toggle {
  display: none;
}

.navbar__item-cart {
  margin-left: 1rem;
}

.navbar__column {
  margin-top: .5rem;
}

.navbar__column-toggleshow {
  display: none;
}

.navbar__item-fluid {
  color: rgba(255, 255, 255, .6);
  text-transform: capitalize;
  padding: 6px 10px;
  border-bottom: 1px solid rgba(255, 255, 255, .5);
  cursor: pointer;
}

.navbar__item-fluid:hover {
  background-color: #fff;
  color: #000;
}

/* @media only screen and (max-width: 768px) { */
@media only screen and (max-width: 992px) {
  .navbar__items {
    display: none;
  }
   
  .navbar__item-brand {
    display: none;
  }

  .navbar__items--right {
    margin-left: 0;
    width: 100%;
  }
  
  .navbar__item-toggleshow {
    display: none;
  }

  .navbar__item-toggle {
    display: initial;
    cursor: pointer;
  }

  .navbar__item-search {
    flex: 1;
    margin-right: 0;
  }

  .navbar__item-cart {
    margin-left: 0;
  }

  .cart-count {
    vertical-align: middle;
    display: none;
  }
}


.banner {
  padding: 2rem 2rem 2rem;
  background: #1D1E20 url("{{ 'banner.png' | asset_url }}") no-repeat bottom right;
  background-size: contain;
  background-blend-mode: exclusion;
  border-radius: .4rem;
}

.banner-title {
  font-family: 'Poppins';
  font-weight: 700;
  font-size: 2.5rem;
  color: #fff;
  margin: 0;
}

.banner-subtitle {
  color: #fff;
  font-weight: 600;
  margin: 0.5rem 0 1rem;
}

.banner-subtitle span {
  color: #aaa;
}

.banner-desc {
  margin: 0;
  color: #fff;
  font-weight: 300;
  font-size: 0.75rem;
  opacity: .5;
  width: 40%;
  padding-left: 1rem;
  border-left: 3px solid #fff;
  line-height: 180%;
}

.brand-mobile {
  display: none;
  text-align: center;
  padding: 1.5rem 0 1rem;
}

@media only screen and (max-width: 992px) {
  .brand-mobile {
    display: block;
  }

  .banner {
    background-image: none;
  }

  .banner-desc {
    width: 100%;
  }
}


.collections {
  margin-top: 3rem;
}

.collection__title {
  font-size: 1.8rem;
  font-weight: 700;
  text-transform: capitalize;
  color: #fff;
  margin: 1rem 0;
}

.card_product {
  margin-bottom: 5rem;
}

.featured_product {
  display: block;
  position: relative;
  background-color: #1D1E20;
  border-radius: .4rem;
  padding: 4rem 2rem 3rem;
}

.featured_product:hover {
  text-decoration: none;
}

.best_seller--tag {
  position: absolute;
  background-color: #000;
  color: #fff;
  border: 2px solid #34383E;
  padding: 0 0.5rem;
  transform: translateY(-100%);
}

.featured_product__title {
  font-size: 1rem;
  font-weight: 300;
  line-height: 160%;
  color: #fff;
  text-align: center;
  margin-top: 1.5rem;
}

.product_name-suffix {
  font-weight: 700;
}

.featured_product-prices {
  text-align: center;
  margin-top: .3rem;
}

.featured_product__price {
  font-size: 1.25rem;
  font-weight: 700;
  color: #fff;
}

.featured_product__compare_price {
  font-size: 0.8rem;
  font-weight: 700;
  color: #aaa;
  margin-left: .25rem;
}

.add_to_cart--button {
  position: absolute;
  background-color: #1d1e20;
  padding: 1rem;
  border: 1px solid #fff;
  border-radius: 0.3rem;
  left: 50%;
  bottom: 0;
  transform: translate(-50%, 50%);
  cursor: pointer;
}

.add_to_cart--button:hover {
  background-color: #fff;
}

.add_to_cart--button:hover path {
  stroke: #000;
}

.add_to_cart--label {
  margin: 0;
  color: rgba(255, 255, 255, .6);
  font-size: .75rem;
  text-transform: capitalize;
  text-align: center;
  position: absolute;
  bottom: -3.5rem;
  transform: translateX(-50%);
  left: 50%;
}


/* Begin Product Page */
.product--page {
  width: 75%;
}

.product--page--upper {
  display: flex;
  flex-direction: row;
  align-items: center;
}

.product--page--image {
  width: 46%;
  margin-right: 4%;
}

.product--page__title {
  font-size: 1.25rem;
  font-weight: 300;
  /* margin-bottom: .1rem; */
  color: #fff;
}

.product--page--name-suffix {
  font-weight: 700;
}

.product--page__price {
  font-size: 1.2rem;
  font-weight: 700;
  color: #fff;
  margin-right: .5rem;
}

.product--page__compare_price {
  font-size: .8rem;
  font-weight: 700;
  color: #aaa;
}

.product--page-taxincluded {
  font-size: .9rem;
  color: #fff;
  margin-top: 1rem;
}

.product--page--pay-buttons {
  display: flex;
  padding-top: 1rem;
}

.product--page--pay-buttons .pay-button {
  font-size: 1rem;
  color: #fff;
  background: #000;
  border: 1px solid #f5f5f5;
  border-radius: .2rem;
  padding: .5rem 1rem;
  margin-right: .5rem;
}

.product--page--pay-buttons .pay-button:hover {
  background-color: #fff;
  color: #000;
}

.product--page--more-options {
  display: block;
  color: #fff;
  font-size: 1rem;
  text-decoration: underline;
}

.product--page--more-options:hover {
  color: #fff;
}

.product--page__description-label {
  font-size: 1.5rem;
  font-weight: 700;
  color: #fff;
  margin: 3rem 0 1rem;
}

.product--page__description {
  font-size: .85rem;
  font-weight: 300;
  color: rgba(255, 255, 255, .8);
}

@media only screen and (max-width: 992px) {
  .product--page {
    width: 100%;
  }

  .product--page--upper {
    flex-direction: column;
  }

  .product--page--image,
  .product--page--summary {
    width: 100%;
    margin-right: 0;
  }

  .product--page--summary {
    margin-top: 1.5rem;
  }

  .product--page--pay-buttons {
    width: 100%;
    flex-direction: column;
  }

  .product--page--pay-buttons .pay-button {
    width: 100%;
    margin-right: 0;
  }

  .product--page__title,
  .product--page-prices,
  .product--page-taxincluded,
  .product--page--more-options {
    text-align: center;
  }
}

/* End Product Page */

/* Begin Breadcrumb */
.bread-crumb {
  display: flex;
  -ms-flex-wrap: wrap;
  flex-wrap: wrap;
  padding: 0.75rem 1rem;
  margin-bottom: 1rem;
  list-style: none;
  background-color: transparent;
  border-radius: 0.25rem;
}

.bread-crumb > li {
  display: inline-block;
  font-size: 1rem;
  color: #aaa;
}

.bread-crumb--home::after {
  padding: 0 .3rem;
  color: #fff;
  content: ">";
}

.bread-crumb .bread-crumb--active {
  font-size: 1.2rem;
  color: #fff;
}
/* End Breadcrumb */

/* Begin Cart Page */
.cart--products {
  display: flex;
  margin-bottom: .8rem;
}

.cart--product-card {
  background-color: #1D1E20;
  border-radius: 0.5rem;
  padding: .5rem;
}

.cart--product-card_header {
  color: #fff;
  font-size: 1.2rem;
  font-weight: 500;
}

.cart--product__summary,
.cart--product__price,
.cart--product__quantity,
.cart--product__total {
  display: flex;
  align-items: center;
  font-size: 1.2rem;
  font-weight: 300;
}

.cart--product__summary {
  width: 35%;
  color: #fff;
}

.cart--product__summary img {
  width: 8rem;
  margin-right: .7rem;
}

.cart--product__price,
.cart--product__quantity {
  width: 20%;
  color: #fff;
}

.cart--product__total {
  width: 25%;
  color: #fff;
  justify-content: space-between;
  padding-right: 1.5rem;
}

.cart--increment,
.cart--decrement {
  font-weight: 300;
}

.cart--increment {
  padding-left: 1rem;
}

.cart--decrement {
  padding-right: 1rem;
}

.icon-close:hover,
.cart--increment:hover,
.cart--decrement:hover {
  cursor: pointer;
}

.cart--subtotal-section {
  display: flex;
  align-items: center;
  font-size: 1.3rem;
  font-weight: 500;
  color: #fff;
}

.cart--subtotal-label {
  width: 75%;
}

.cart--products-mobile {
  display: none;
  margin-bottom: 1.5rem;
  color: #fff;
  font-size: 1.25rem;
  font-weight: 500;
}

.cart--product--card-mobile {
  background-color: #1D1E20;
  border-radius: .4rem;
  padding: 1.5rem;
}

.cart--product--mobile__summary,
.cart--product--mobile__price,
.cart--product--mobile__quantity,
.cart--product--mobile__total {
  display: flex;
  align-items: center;
  margin-bottom: 1rem;
}

.cart--product--mobile__price,
.cart--product--mobile__quantity,
.cart--product--mobile__total {
  justify-content: space-between;
}

.cart--quantity--buttons {
  display: flex;
  align-items: center;
}

.cart--product--mobile__summary img {
  width: 12rem;
  margin-right: 1rem;
}

.cart-refresh--button,
.cart-continue--button {
  font-family: 'Poppins';
  font-style: normal;
  font-weight: 400;
  font-size: .8rem;
  line-height: 120%;
  text-transform: capitalize;
  background-color: #fff;
  color: #000;
  border: 1px solid rgba(255, 255, 255, .5);
  border-radius: .4rem;
  padding: 0 1.2rem;
  margin-right: .7rem;
}

.cart-refresh--button svg {
  width: .8rem;
  height: .8rem;
  line-height: 120%;
}

.cart--buttons {
  display: inline-flex;
  flex-wrap: wrap;
  margin-bottom: 4rem;
  margin-top: 2rem;
}

.terms--agreement {
  margin-top: 1.5rem;
}

.cart--terms--agreement {
  font-size: 1rem;
  color: #fff;
}

@media only screen and (max-width: 992px) {
  .cart--products {
    display: none;
  }

  .cart--product__summary {
    width: 35%;
  }
  
  .cart--product__price,
  .cart--product__quantity {
    width: 20%;
  }
  
  .cart--product__total {
    width: 25%;
  }

  .cart--products-mobile {
    display: block;
  }

  .cart--subtotal-section {
    justify-content: space-between;
    font-size: 1.5rem;
  }

  .cart--subtotal-label {
    width: auto;
  }

  .cart--buttons {
    width: 100%;
  }

  .cart-refresh--button,
  .cart-continue--button,
  .cart-checkout--button {
    font-size: 1rem;
    padding-top: 1rem;
    padding-bottom: 1rem;
  }

  .cart-continue--button {
    flex: 1;
    margin-right: 0;
  }
  
  .cart-checkout--button {
    width: 100%;
    margin-top: 1rem;
  }

}
/* End Cart Page */
